apiVersion: v1
kind: Service
metadata:
  name: mdai-collector-service
  labels:
    app: mdai-collector
spec:
  selector:
    app: mdai-collector
  ports:
    - port: 4317
      targetPort: otlp-grpc
      protocol: TCP
      name: otlp-grpc
  type: ClusterIP
